# RAGFlow 添加新模型提供商指南

本文档详细介绍如何在RAGFlow中添加您公司的自定义模型提供商。

## 概述

在RAGFlow中添加新的模型提供商需要修改以下几个核心组件：

1. **配置文件** - 定义新提供商的基本信息和支持的模型
2. **后端代码** - 实现模型调用逻辑
3. **前端代码** - 添加UI界面支持

## 1. 配置文件修改

### 1.1 LLM工厂配置文件

**文件路径**: `conf/llm_factories.json`

这是最重要的配置文件，定义了所有支持的模型提供商。

```json
{
    "factory_llm_infos": [
        // 现有的提供商配置...
        {
            "name": "YourCompany",  // 您公司的名称
            "logo": "",  // 可选：公司logo URL
            "tags": "LLM,TEXT EMBEDDING,CHAT",  // 支持的功能标签
            "status": "1",  // 状态：1为启用
            "llm": [
                {
                    "llm_name": "your-model-v1",  // 模型名称
                    "tags": "LLM,CHAT,8K",  // 模型标签
                    "max_tokens": 8192,  // 最大token数
                    "model_type": "chat",  // 模型类型：chat/embedding/rerank/image2text/speech2text/tts
                    "is_tools": true  // 是否支持工具调用
                },
                {
                    "llm_name": "your-embedding-v1",
                    "tags": "TEXT EMBEDDING,4K", 
                    "max_tokens": 4096,
                    "model_type": "embedding",
                    "is_tools": false
                }
            ]
        }
    ]
}
```

### 1.2 支持的模型类型

- `chat`: 对话模型
- `embedding`: 文本嵌入模型  
- `rerank`: 重排序模型
- `image2text`: 图像转文本模型
- `speech2text`: 语音转文本模型
- `tts`: 文本转语音模型

## 2. 后端代码修改

### 2.1 聊天模型实现

**文件路径**: `rag/llm/chat_model.py`

如果您的模型是聊天模型，需要添加一个新的类：

```python
class YourCompanyChat(Base):
    _FACTORY_NAME = "YourCompany"

    def __init__(self, key, model_name, base_url="https://api.yourcompany.com/v1", **kwargs):
        if not base_url:
            base_url = "https://api.yourcompany.com/v1"
        super().__init__(key, model_name, base_url, **kwargs)

    def _clean_conf(self, gen_conf):
        # 清理和转换配置参数
        for k in list(gen_conf.keys()):
            if k not in ["temperature", "top_p", "max_tokens"]:
                del gen_conf[k]
        return gen_conf

    def _chat(self, history, gen_conf={}, **kwargs):
        # 实现具体的聊天逻辑
        response = self.client.chat.completions.create(
            model=self.model_name,
            messages=history,
            **gen_conf
        )
        ans = response.choices[0].message.content
        return ans, self.total_token_count(response)
```

### 2.2 嵌入模型实现

**文件路径**: `rag/llm/embedding_model.py`

如果您的模型是嵌入模型：

```python
class YourCompanyEmbedding(Base):
    _FACTORY_NAME = "YourCompany"

    def __init__(self, key, model_name, base_url="https://api.yourcompany.com/v1", **kwargs):
        if not base_url:
            base_url = "https://api.yourcompany.com/v1"
        super().__init__(key, model_name, base_url, **kwargs)

    def encode(self, texts, batch_size=32):
        # 实现文本编码逻辑
        # 返回 (embeddings, token_count)
        pass
```

### 2.3 API密钥处理

**文件路径**: `api/apps/llm_app.py`

在 `add_llm` 函数中添加特殊的API密钥处理逻辑（如果需要）：

```python
elif factory == "YourCompany":
    # 如果需要特殊的API密钥格式
    api_key = apikey_json(["your_api_key", "your_secret_key"])
```

## 3. 前端代码修改

### 3.1 添加LLM工厂枚举

**文件路径**: `web/src/constants/llm.ts`

```typescript
export enum LLMFactory {
  // 现有的枚举值...
  YourCompany = 'YourCompany',
}

export const IconMap = {
  // 现有的映射...
  [LLMFactory.YourCompany]: 'your-company-icon',
};
```

### 3.2 创建自定义模态框（可选）

如果您的模型需要特殊的配置界面，可以创建自定义模态框：

**文件路径**: `web/src/pages/user-setting/setting-model/your-company-modal/index.tsx`

```typescript
const YourCompanyModal = ({
  visible,
  hideModal,
  onOk,
  loading,
  llmFactory,
}: IModalProps<IAddLlmRequestBody> & { llmFactory: string }) => {
  const [form] = Form.useForm<FieldType>();
  const { t } = useTranslate('setting');

  const handleOk = async () => {
    const values = await form.validateFields();
    const data = {
      ...values,
      llm_factory: llmFactory,
    };
    onOk?.(data);
  };

  return (
    <Modal
      title={t('addLlmTitle', { name: llmFactory })}
      open={visible}
      onOk={handleOk}
      onCancel={hideModal}
      okButtonProps={{ loading }}
    >
      <Form form={form} layout="vertical">
        <Form.Item
          label={t('modelType')}
          name="model_type"
          rules={[{ required: true }]}
        >
          <Select>
            <Option value="chat">chat</Option>
            <Option value="embedding">embedding</Option>
          </Select>
        </Form.Item>
        
        <Form.Item
          label={t('modelName')} 
          name="llm_name"
          rules={[{ required: true }]}
        >
          <Input />
        </Form.Item>

        <Form.Item
          label={t('apiKey')}
          name="api_key"
          rules={[{ required: true }]}
        >
          <Input.Password />
        </Form.Item>

        <Form.Item
          label={t('baseUrl')}
          name="api_base"
        >
          <Input placeholder="https://api.yourcompany.com/v1" />
        </Form.Item>
      </Form>
    </Modal>
  );
};
```

### 3.3 注册模态框

**文件路径**: `web/src/pages/user-setting/setting-model/index.tsx`

```typescript
// 在模态框映射中添加
const ModalMap = {
  // 现有的映射...
  [LLMFactory.YourCompany]: showYourCompanyModal,
};

// 添加Hook和状态管理
const {
  visible: yourCompanyVisible,
  hideModal: hideYourCompanyModal,
  showModal: showYourCompanyModal,
} = useSetModalState();

// 在JSX中添加模态框组件
<YourCompanyModal
  visible={yourCompanyVisible}
  hideModal={hideYourCompanyModal}
  onOk={onYourCompanyAddingOk}
  loading={yourCompanyAddingLoading}
  llmFactory={LLMFactory.YourCompany}
/>
```

## 4. 特殊配置支持

### 4.1 自定义认证方式

如果您的API需要特殊的认证方式（如多个密钥），可以在后端处理：

```python
elif factory == "YourCompany":
    api_key = apikey_json(["access_key", "secret_key", "region"])
```

### 4.2 本地模型支持

如果是本地部署的模型，需要在前端标记为本地模型：

```typescript
export const LocalLlmFactories = [
  // 现有的本地模型...
  LLMFactory.YourCompany,
];
```

## 5. 图标资源

### 5.1 添加公司图标

将您公司的图标文件放置在：`web/src/assets/icon/llm/` 目录下

图标命名规则：`your-company-icon.svg` 或 `your-company-icon.png`

## 6. 完整示例

假设要添加一个名为"MyCompany"的新提供商：

### 6.1 配置文件示例

```json
{
  "name": "MyCompany",
  "logo": "",
  "tags": "LLM,TEXT EMBEDDING,CHAT",
  "status": "1", 
  "llm": [
    {
      "llm_name": "mycompany-chat-v1",
      "tags": "LLM,CHAT,8K",
      "max_tokens": 8192,
      "model_type": "chat",
      "is_tools": true
    }
  ]
}
```

### 6.2 后端实现示例

```python
class MyCompanyChat(Base):
    _FACTORY_NAME = "MyCompany"

    def __init__(self, key, model_name, base_url="https://api.mycompany.com/v1", **kwargs):
        if not base_url:
            base_url = "https://api.mycompany.com/v1"
        super().__init__(key, model_name, base_url, **kwargs)
```

### 6.3 前端枚举示例

```typescript
export enum LLMFactory {
  MyCompany = 'MyCompany',
}

export const IconMap = {
  [LLMFactory.MyCompany]: 'mycompany',
};
```

## 7. 测试验证

### 7.1 重启服务

修改完成后，需要重启RAGFlow服务：

```bash
# 停止现有服务
pkill -f "ragflow_server.py|task_executor.py"

# 重新启动
cd /path/to/ragflow
source .venv/bin/activate
export PYTHONPATH=$(pwd)
bash docker/launch_backend_service.sh
```

### 7.2 验证步骤

1. 登录RAGFlow管理界面
2. 进入"模型提供商"页面
3. 查看是否出现了新的提供商选项
4. 尝试添加和配置新的模型
5. 测试模型调用是否正常

## 8. 注意事项

### 8.1 命名规范

- 工厂名称要与配置文件中的 `name` 字段完全一致
- 类名中的 `_FACTORY_NAME` 要与工厂名称匹配
- 前端枚举值要与后端工厂名称匹配

### 8.2 错误处理

- 确保在模型类中正确处理异常
- 返回标准的错误格式：`"**ERROR**: 错误信息"`

### 8.3 性能考虑

- 对于高并发场景，考虑添加连接池
- 实现合适的重试机制
- 添加超时控制

## 9. 常见问题

### 9.1 模型不显示

检查：
- `llm_factories.json` 文件格式是否正确
- 工厂名称是否在前后端保持一致
- 是否重启了服务

### 9.2 API调用失败

检查：
- API密钥格式是否正确
- base_url 是否可访问
- 模型名称是否正确

### 9.3 前端界面异常

检查：
- 前端枚举是否正确添加
- 图标文件是否存在
- 组件是否正确注册

## 10. 总结

添加新的模型提供商主要涉及三个层面的修改：

1. **配置层**：在 `llm_factories.json` 中定义提供商信息
2. **业务层**：在后端实现具体的模型调用逻辑
3. **展示层**：在前端添加相应的UI支持

按照本指南逐步操作，您就能成功地将公司的模型集成到RAGFlow中。

如果在实施过程中遇到问题，建议：
1. 参考现有提供商的实现方式
2. 查看日志文件排查错误
3. 逐步测试每个组件的功能

祝您集成成功！